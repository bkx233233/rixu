# 进度

## 2026-08-01

- 确定项目名为“日序”，技术方案为 Flutter + Supabase。
- 本机检查确认尚未安装 Flutter。
- 建立项目骨架和首份数据库迁移，等待 Flutter 与 Supabase 配置完成后进入功能实现。
- Flutter SDK 已安装到 `.tools/flutter`，但 `flutter --version` 和 `flutter create` 都卡在自身工具的依赖下载，Android/Web 目录尚未生成。
- 完成登录、注册、退出登录和主导航代码，等待云端配置后联调。
- 已收到 Supabase 项目连接信息，用户确认已在 SQL Editor 执行数据库迁移，等待 Flutter SDK 后做真实连接验证。
- Flutter 已完成初始化并生成 Android/Web 平台；本地沙箱无法读取用户 Pub 缓存，分析器暂不能运行。
- 日程页面已支持按日期读取、添加任务和标记完成。
- 健康页面已支持训练中/休息中、手动饮食记录、体重记录和近 30 天趋势图。
- Dart 语法格式检查通过；完整依赖分析和 APK 构建仍需在可读取 Pub 缓存的终端执行。
- 修复登录/注册无反馈问题：增加按钮加载状态、15 秒超时、认证异常和通用异常提示，并在成功后显示确认消息。
- 统一用户提示为中文，补充 Supabase 常见英文错误的中文转换；网页标题和 Android 应用名改为“日序”。
- 注册成功/失败反馈改为明显的状态卡；注册成功增加“重新发送确认邮件”和“去登录”。
- 日程新增日/周切换、每日总结编辑、本周/本月目标添加和完成标记。
- 健身中新增动作录入；休息中训练区域灰显，客户端和新增数据库迁移均拒绝写入训练动作。
- 添加 GitHub Actions + Netlify 网页发布配置，正式网页不依赖本地电脑持续开机。
- 修复每日总结组件不接收 `key` 导致的 Flutter 热重启编译错误。
- 日程和健康页面已订阅 Supabase Realtime；训练动作表新增实时发布迁移，等待用户在 Supabase SQL Editor 执行后验证多设备自动刷新。
- 日程读取显式按开始时间升序排列，日视图和周视图均增加删除确认；训练动作新增按部位的常用动作和常用组次数快捷选择。
- 新增个人常用食物库：用户可新建、删除和选择食物；选择后会按份量自动填入营养数据，等待执行 `202608020003_food_items.sql` 后联调。
- 修复训练快捷组次数和常用食物列表的两处 Dart 编译错误。
- 增强训练保存错误提示；下一次重试会显示更具体的中文原因。
- 针对“健身中仍无法保存训练动作”，新增训练保护函数修复迁移 `202608020004_fix_training_guard.sql`。
- 完成 0.2 版本需求盘点。食物库、营养资料、目标计算和统一编辑删除均需数据库迁移，按项目规则等待用户确认后实施。
- 日程模块已实现日程、每日总结和周期目标的编辑删除；日程编辑可调整日期、开始结束时间、名称和备注，目标编辑可调整名称与起止日期。正常刷新保留原内容并显示局部加载状态。
- 完成 0.2 客户端：每 100 克食物模型、核心记录编辑删除、营养目标与摘要、我的体重趋势、底部导航状态保留、Android 提醒及小组件。
- `flutter build web --release` 和 `flutter build apk --release` 均已通过；APK 位于 `build/app/outputs/flutter-apk/app-release.apk`。
- 等待在 Supabase SQL Editor 执行 `202608020005_health_v2.sql`，客户端新字段依赖该迁移。
- 日程小组件已升级为按尺寸展示下一项或全部任务列表；日程提醒升级为精确闹钟、全屏响铃和稍后提醒。Android release APK 构建通过。
