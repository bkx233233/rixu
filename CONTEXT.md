# 当前进度

正在从零建立“日序”第一版。目标是 Android 与网页共用 Flutter 代码，通过 Supabase 提供账号、云端数据库与多设备同步。

# 最近进展

- 已修复网页空白页：Android 闹铃与桌面小组件服务原先在 Web 环境读取原生平台信息，导致应用在 `runApp` 前终止。现在 Web 启动不会初始化闹铃，两个原生服务也会先跳过 Web。等待重新构建并发布验证。

# 当前状态

- 0.2 客户端功能已完成并通过 Web、Android release 构建；`202608020005_health_v2.sql` 尚待在 Supabase SQL Editor 执行。
- 线上 Netlify 仍是 `origin/main` 的旧代码，尚未包含本地 0.2 的“我的”资料、营养目标、体重趋势和完整编辑删除功能；本次将修复缓存后推送发布。
- 现有食物库的营养值以“默认份量”保存。迁移时仅能自动换算单位为 g 的旧食物；其他单位无法严谨换算，必须保留原数据并提示用户补充每 100 克营养数据，不能擅自估算。
- 当前页面卡顿由页面切换时重新创建页面、读取请求串行执行、Realtime 回调触发整页 FutureBuilder 加载共同造成。下一版改为应用级内存缓存、并行查询、写入后局部更新和 Realtime 局部失效，首次无缓存时才显示骨架屏。

- Flutter SDK 已安装并已完成 Android/Web 工程初始化；完整构建仍需在用户终端验证，因为 Codex 沙箱无法读取用户 Pub 缓存。
- 已取得 Supabase 项目地址和 Publishable 公开连接密钥，但密钥只通过启动参数使用，不写入代码仓库。
- Flutter 连接地址使用项目根地址 `https://xyqjlhvjjntbfzztehpv.supabase.co`，不带 `/rest/v1/`。
- 日程和健康页面已订阅 Supabase Realtime；需要用户确认 202608020002、202608020003、202608020004 三份迁移执行成功后，其他在线设备的写入会自动刷新当前页面。
- 日程统一按开始时间从早到晚显示，日视图和周视图均可确认删除；训练动作支持部位、常用动作和组次数快捷选择，并保留自定义输入。
- 饮食支持用户维护自己的常用食物；选择后按份量自动换算营养数据，饮食记录仍保存当次营养快照。
- Android 日程小组件已升级：小尺寸显示下一项任务，中大尺寸显示当天全部任务和完成进度；数据仍经 Kotlin 通道写入 SharedPreferences。当前沙箱无法读取 Flutter SDK 的 Git 元数据，Android 构建需在本机终端验证。

# 关键决定

- 营养目标采用 Mifflin-St Jeor 基础代谢公式和活动系数计算。首次登录资料需补充生理性别、出生日期、身高、当前体重、活动水平及热量缺口；未完成资料不生成伪精确建议。
- Android 通知和桌面小组件均可实现，但它们是 Android 原生能力。通知在当前手机本地安排；若要应用关闭后也在其他设备实时更新小组件，需要后续加入 FCM 云推送。
- Android 小组件使用项目自己的 Kotlin 通道和 SharedPreferences 实现，避免第三方小组件依赖在中文路径下触发原生构建失败。
- 日程小组件已改为：小尺寸显示下一项任务，中/大尺寸展示可滚动的当天任务列表。闹铃改为 Android `AlarmManager` 精确闹钟、响铃震动、全屏关闭/5 分钟稍后提醒，并在重启后恢复。

- 第一版使用 Flutter + Supabase，避免一开始维护自建服务器。
- 云端数据库是唯一可信数据来源；本地只做缓存。
- 体重缺失时不写入前一天体重，避免伪造历史记录。
- 当前功能采用联网读取云端数据；周/月视图、目标、每日总结和训练动作第一版已加入。
- 登录和注册必须对加载、成功、失败、超时都给出可见反馈；当前已补齐这条规则。
- 所有用户可见提示统一使用中文；服务端英文错误通过统一转换器转成中文。
- 注册反馈采用第一版方案：表单内状态卡，包含图标、状态标题、说明和下一步操作。
- 网页正式发布使用 Netlify，不依赖本地电脑持续开机。
- 已修复每日总结组件不接收 `key` 导致 Flutter 热重启失败的问题；用户可在运行窗口按大写 `R` 重新启动。
- 训练动作表的实时推送、休息日训练保护和训练保护修复仍等待用户在 Supabase SQL Editor 执行迁移。
- 常用食物使用 `food_items` 表且不内置未经核验的营养数据；用户仍需执行 `202608020003_food_items.sql`。
- 修复训练快捷组次数的 Dart 记录类型不一致，以及常用食物列表分隔回调参数重名导致的热重启编译错误。
- 训练动作保存失败时改为显示状态、迁移、权限和数据格式对应的中文原因，避免统一显示模糊的“保存失败”。
- 截图确认训练状态已是“健身中”但保存仍失败；新增 `202608020004_fix_training_guard.sql`，用明确变量名重建训练保护触发器。
- 发布配置已兼容 GitHub 的 `main` 和 `master` 分支；当前仍缺 GitHub 远程仓库、Netlify 站点和账号授权，尚未完成首次上线。
- GitHub 远程仓库现为 `bkx233233/rixu`；远程 `main` 仍是初始提交，本地 `main` 的合并提交尚未推送，需推送后才能触发网页发布。
- 用户已授权安装 GitHub CLI 并推送 `main`，但 Codex 的外部安装审批服务两次返回 503，当前无法执行安装或网络推送。
- 已找到用户安装的 GitHub CLI；本地 `.env.github` 中令牌格式完整，但 Codex 沙箱无法连接 GitHub，且外网验证审批服务返回 503，令牌有效性与推送状态暂无法确认。
- 日程模块已补齐日程、周期目标和每日总结的编辑/删除；正常刷新保留已加载内容，不再整页转圈，等待用户本机验证。
