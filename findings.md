# 已确认事项

- Flutter 是客户端开发工具，生成的 Android 应用运行在手机上，网页应用运行在浏览器中。
- Supabase 提供云端账号、数据库、权限和实时同步，不需要第一版自建传统服务器。
- Flutter SDK 未安装，当前无法执行构建和自动化测试。
- Supabase 项目尚未完成配置，当前不能进行真实账号和同步测试。
- Flutter SDK 文件已存在，但首次初始化依赖无法通过当前网络沙箱下载；必须先在可访问包源的本机终端完成一次初始化。
- Supabase 项目已创建；客户端连接需要使用项目根 URL 和 Publishable 公开密钥，不能使用数据库密码或 secret 密钥。
- 用户已完成 Flutter 初始化和 `pub get`，但依赖缓存位于用户目录，Codex 沙箱无权读取；后续构建需用户终端执行，或提供项目内可读缓存。
